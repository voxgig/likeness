@"./base.aon"

ErrorDef: type(close({
  code: $.Code
  exit: $.Exit

  # One line, in the present tense, saying what happened.
  means: string

  partial: boolean

  # Whether retrying the same command unchanged could plausibly succeed. An
  # agent reads this instead of guessing from the message text.
  retryable: boolean
}))

error: { &: $.ErrorDef }
